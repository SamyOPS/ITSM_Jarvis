export function App() {
  return (
    <main className="app-shell">
      <section className="app-card">
        <p className="eyebrow">TikIA</p>
        <h1>Frontend skeleton ready</h1>
        <p className="copy">
          This frontend stays intentionally minimal. Add pages, features, and
          shared components later without changing the base structure.
        </p>
      </section>
    </main>
  );
}
